import React from 'react';
// import { CategoriesBar, VideoCard } from '../components';
import './_home.scss';

const Home = () => (
  <>
    {/* 
        {[...new Array(20)].map(() => (
          <Col lg={3} md={4}>
            <VideoCard />
          </Col>
        ))}
       */}
  </>
);

export default Home;
